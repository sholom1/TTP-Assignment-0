{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Script: test-all",
            "request": "launch",
            "command": "npm run test-all",
            "cwd": "${workspaceFolder}"
        }
    ]
}